export const metadata = { title: 'Clinked-Calendly Sync' };
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return <html lang="en"><body>{children}</body></html>;
}
